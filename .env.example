# Chaves só no servidor. Nunca use o prefixo VITE_ (isso jogaria a chave no navegador).
# Copie este arquivo para .env e cole a chave. O arquivo .env não vai para o GitHub.

GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
