# Chaves da I.A. — só no servidor (nunca VITE_*, nunca no GitHub)

# Local (npm run dev): cole aqui
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=

# Produção (GitHub Pages): URL do backend no Render, sem barra no final
# VITE_API_URL=https://maya-api.onrender.com
